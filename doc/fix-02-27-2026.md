# High-Severity Audit Findings — Remediation Summary

All **11 High-Severity findings** have been resolved.  
This document summarizes the fixes completed during this session.

---

## Completed High-Severity Fixes (S2–S6)

| #  | Finding | Fix |
|----|----------|-----|
| **S2** | 4MB max for all message types enables memory amplification | Implemented per-type message size limits:<br>• Proposals → 2MB<br>• Votes → 4KB<br>• Timeouts → 4KB<br><br>Enforced in `DecodeEnvelope`. |
| **S3** | No RPC rate limiting on HTTP gateway | Added per-IP token-bucket rate limiter (100 requests / 10s) as middleware in `rpc/ratelimit.go`. |
| **S4** | HighQC from timeout messages accepted without verification | Updated `TimeoutCollector.HighestQC()` to call `QC.Verify()` before accepting any QC.<br>Added test to ensure forged QCs are rejected. |
| **S5** | Telemetry disabled by default | Changed `TelemetryConfig.Enabled` default value to `true`. |
| **S6** | Admin API hardcoded to `127.0.0.1:26661` | Added `AdminAddr` to `RPCConfig`, wired through `node.go`.<br>Defaults to loopback address. |

---

## Validation

- All **14 packages** pass with `-race`.
- The full set of High-Severity fixes (**H1–H5, S1–S6**) from the audit is now complete.

---

**Status:** ✅ High-severity audit remediation complete.
